[package]
name = "canopy-core"
version = "0.1.0"
edition = "2021"
description = "Shared data types (structs, enums, serde) for the Canopy planning pipeline"
license.workspace = true

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_yaml = "0.9"
thiserror = "1"
